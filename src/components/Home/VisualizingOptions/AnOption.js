import { Box, Typography } from '@mui/material';
import React from 'react';

export default function AnOption (){
  return (
    <Box sx={{}}>
        <Typography variant="h4">An Option</Typography>
    </Box>
  );
};